import { Appointments.Routes } from './appointments.routes';

describe('Appointments.Routes', () => {
  it('should create an instance', () => {
    expect(new Appointments.Routes()).toBeTruthy();
  });
});
